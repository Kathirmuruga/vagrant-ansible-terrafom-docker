name: Test Self-Hosted Runner

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run a simple test
        run: |
          echo "✅ GitHub self-hosted runner is working!"
          uname -a
          whoami
